const accountId=14452
let accountEmail="piyush003@gmail.com"
var accountPassword="121212"
let accountState


accountCity="indore"

/*
let and var are different because of the scope we cant use the same name in different scopes if we use keyword let
so never use var keyword
*/


console.table([accountId,accountEmail,accountPassword,accountCity,accountState])